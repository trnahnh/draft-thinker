# Draft-Thinker Research Showcase

Static Next.js site presenting Draft-Thinker's calibration results, entropy analysis, and architecture.

## Run

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## Build

```bash
npm run build
```

Output in `.next/`. Fully static, no server-side data fetching.
